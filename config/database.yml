# Postgress version
#   gem install pg
#
#   Ensure the postgres gem is defined in your Gemfile
#   gem 'pg'
development:
    adapter: postgresql
    encoding: utf8
    database: creative_development
    pool: 5
    username: aleph
    password: 

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test: &TEST
    adapter: postgresql
    encoding: utf8
    database: creative_test
    pool: 5
    username: aleph
    password: 

production:
    adapter: postgresql
    encoding: utf8
    database: creative_production
    pool: 5
    username: <%= ENV['PG_USERNAME'] %>
    password: <%= ENV['PG_PASSWORD'] %>

cucumber:
  <<: *TEST

